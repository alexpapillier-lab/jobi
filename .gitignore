# Logs
logs
*.log
notarize-log.txt
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ENV
.env
.env.*
!.env.example

# OTA signing secrets (local only)
docs/OTA_SIGNING_SECRETS.md

# DB backup (obsahuje citlivé cesty/URL)
backup/
.env.backup

# Tauri
src-tauri/target
src-tauri/gen

# Tooling cache
.vite
.cache

# Coverage
coverage

# Build / release artefakty (zip, test release složka)
jobi-test-release/
jobi-test-release.zip
jobi-jobidocs-instalace.zip
Releases/
src-tauri/jobi-universal.zip

# Claude Code – worktrees / cache (celé duplicitní repo; nebuildí se z nich)
web/.claude/

# Supabase CLI cache (vzniká při práci s CLI, nepatří do repa)
supabase/.temp/
supabase/.branches/

# Deno lock. Vzniká sám při `deno check` nad edge funkcemi, ale nikdo ho
# nečte: v repu není deno.json a Supabase CLI si závislosti při nasazení
# bundluje po svém. Jen by se měnil pod rukama. Kdybychom verze opravdu
# chtěli pinovat, chce to vědomě přidat deno.json a kontrolu v CI.
deno.lock

# Web build
dist-web
dist-site

# Stažená data a vygenerované SQL pro import katalogu – dump cizího API
# a SQL s ID konkrétního servisu, do repa nepatří (viz README v té složce).
scripts/import-zakazkovylist/catalog_*.json
scripts/import-zakazkovylist/import_katalog.sql

# Playwright
test-results/
playwright-report/

# Logs
logs
*.log
notarize-log.txt
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ENV
.env
.env.*
!.env.example

# OTA signing secrets (local only)
docs/OTA_SIGNING_SECRETS.md

# DB backup (obsahuje citlivé cesty/URL)
backup/
.env.backup

# Tauri
src-tauri/target
src-tauri/gen

# Tooling cache
.vite
.cache

# Coverage
coverage

# Build / release artefakty (zip, test release složka)
jobi-test-release/
jobi-test-release.zip
jobi-jobidocs-instalace.zip
Releases/
src-tauri/jobi-universal.zip

# Claude Code – worktrees / cache (celé duplicitní repo; nebuildí se z nich)
web/.claude/

# Supabase CLI cache (vzniká při práci s CLI, nepatří do repa)
supabase/.temp/linked-project.json

# Web build
dist-web
dist-site

name: Build Windows

# Windows build nejde udělat z macOS – ring (krypto v updateru) potřebuje
# Windows C hlavičky. Proto se staví tady, na windows-latest runneru.
#
# Potřebné secrets v nastavení repozitáře:
#   VITE_SUPABASE_URL          – Vite ho zapéká do buildu
#   VITE_SUPABASE_ANON_KEY     – veřejný klíč, v hotové aplikaci je stejně
#   VITE_ROOT_OWNER_ID         – volitelné
#   TAURI_SIGNING_PRIVATE_KEY  – minisign klíč pro OTA (stejný jako u macOS)
#   TAURI_SIGNING_PRIVATE_KEY_PASSWORD
#
# Aplikace NENÍ podepsaná Authenticode certifikátem, takže SmartScreen
# při instalaci ohlásí neznámého vydavatele. To je zatím záměr.

on:
  workflow_dispatch:
    inputs:
      include_arm64:
        description: "Postavit i ARM64 verzi (diagnostika – běží nativně v Parallels na Apple Silicon)"
        type: boolean
        default: false
  push:
    tags: ["v*"]

jobs:
  jobi:
    name: Jobi (Tauri, NSIS installer)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - uses: dtolnay/rust-toolchain@stable

      - uses: swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Instalace závislostí
        run: npm ci

      - name: Build
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
          VITE_ROOT_OWNER_ID: ${{ secrets.VITE_ROOT_OWNER_ID }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        # --bundles nsis explicitně: tauri.conf.json má targets "app" (macOS),
        # takže Windows target se říká tady a konfigurace zůstává nedotčená.
        run: |
          npm run build:prod
          npx tauri build --bundles nsis

      - name: Uložit installer
        uses: actions/upload-artifact@v4
        with:
          name: jobi-windows
          path: |
            src-tauri/target/release/bundle/nsis/*.exe
            src-tauri/target/release/bundle/nsis/*.sig
          if-no-files-found: error

  jobidocs:
    name: JobiDocs (Electron, NSIS + portable)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Instalace závislostí
        working-directory: jobidocs
        run: npm ci

      # Skripty copy-app-icon / copy-tray-icon používají unixové mkdir -p a cp.
      # shell: bash na ně nestačí – npm si package skripty spouští VLASTNÍM
      # shellem, kterým je na Windows cmd.exe ("The syntax of the command is
      # incorrect"). Proto npm přesměrujeme na Git Bash, který na runneru je.
      # Skripty tím zůstávají beze změny a odladěný macOS build se nedotkne.
      #
      # build:electron (ne build) – jen ten kompiluje electron/main.ts přes
      # tsconfig.electron.json do dist-electron, které electron-builder potřebuje.
      - name: Build
        working-directory: jobidocs
        shell: bash
        run: |
          npm config set script-shell "C:\\Program Files\\Git\\bin\\bash.exe"
          npm run build:electron
          npx electron-builder --win --publish never

      - name: Uložit installer
        uses: actions/upload-artifact@v4
        with:
          name: jobidocs-windows
          path: |
            jobidocs/release/*.exe
          if-no-files-found: error

  # ARM64 je zatím DIAGNOSTICKÝ build, ne produkt.
  #
  # Zákazníkům stačí x64 – ten na Windows ARM funguje přes emulaci Prism.
  # Nativní ARM64 build slouží k tomu, aby šlo v Parallels na Apple Silicon
  # odlišit, kolik sekání dělá emulace a kolik samotná aplikace.
  #
  # Proto se nespouští automaticky: zaškrtni "include_arm64" při ručním
  # spuštění. Když runner windows-11-arm nebude k dispozici, spadne jen
  # tenhle job a x64 build to neovlivní.
  jobi-arm64:
    name: Jobi ARM64 (diagnostika)
    if: inputs.include_arm64
    runs-on: windows-11-arm
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - uses: dtolnay/rust-toolchain@stable

      - uses: swatinem/rust-cache@v2
        with:
          workspaces: src-tauri
          key: arm64

      - name: Instalace závislostí
        run: npm ci

      - name: Build
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
          VITE_ROOT_OWNER_ID: ${{ secrets.VITE_ROOT_OWNER_ID }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        run: |
          npm run build:prod
          npx tauri build --bundles nsis

      - name: Uložit installer
        uses: actions/upload-artifact@v4
        with:
          name: jobi-windows-arm64
          path: |
            src-tauri/target/release/bundle/nsis/*.exe
            src-tauri/target/release/bundle/nsis/*.sig
          if-no-files-found: error
